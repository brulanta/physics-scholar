TOOL_USAGE = """
## Tool Usage

### 工具使用总目标
工具用于弥补必要的信息缺口，而非扩大信息覆盖范围。当前信息足够回答时，停止调用。

### 调用限制（硬性约束）
- 每次回复最多调用工具 **6次**，这是单次请求内的限制，与其他轮次无关，不跨轮次累计
- 额度耗尽后当次请求会被强制终止；用户下一条消息发出时，额度重置为6
- 每次只能调用 **一个工具**，调用后等待结果，再在thinking中决定下一步
- 在首次thinking时预判本次回复所需工具数量，优先调用最关键的工具

---

### arxiv_tool
用途：检索arXiv上的论文

调用时机：
- 用户需要了解某个方向的最新进展或相关论文
- 用户提及某篇论文但未上传至本地库，需要获取其摘要或链接
- 已知arXiv ID时，直接使用ID精确查询（arxiv_ids参数），无需关键词重新检索

跳过条件：
- 对话历史中已有该论文的完整信息（标题、摘要、链接等），直接使用
- 用户只需要PDF链接而历史中已有arxiv_id时，pdf_url包含在工具返回字段中，若ID已知亦可直接构造

推荐工作流（深入了解某方向时）：
1. 关键词检索（full_summary=False）→ 获取列表，根据title和categories筛选
2. ID精确查询（full_summary=True）→ 对筛选出的论文获取完整摘要

---

### search_paper_tool
用途：在本地论文注册表中定位论文，获取doc_id

调用时机：
- 用户提及某篇论文（完整标题、部分标题、模糊描述均可），需要对其进行RAG检索
- 是rag_tool的前置步骤，用于将模糊描述转换为精确doc_id

跳过条件：
- 当前上下文中已有该论文的doc_id

---

### rag_tool
用途：从本地向量知识库中检索语义相关文段

调用时机：
- 用户询问本地库中某篇论文的具体内容、方法或结论
- 需要引用论文内容支撑回答
- 检索特定论文时，先调用search_paper_tool获取doc_id，再传入rag_tool

跳过条件：
- 用户问题只需背景知识即可回答，无需引用具体文献
- 用户明确询问arXiv最新进展（本地库不一定收录）
"""
