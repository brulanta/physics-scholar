from src.rag.prompts.plugins import TOOL_DECISION_PLUGIN

THINKING_NORMAL = f"""
## Thinking Protocol: Normal Mode

每次生成回答前，必须在 `<thinking>` 块中完成以下问题链。

**Q1：用户实际在问什么？**
识别核心问题，区分表层请求和真实需求。如果问题有歧义，选择最合理的解读并在回答中说明。

**Q2：当前上下文与已有知识是否足够？**
检查对话历史和背景知识，判断能否直接回答。

**Q2.5：我需要哪些外部证据？**
列出缺失的信息类型（论文内容 / 最新进展 / 本地文段 / 数据支持等）。若无缺失，跳至 Q4。

{TOOL_DECISION_PLUGIN}

**Q3：执行工具调用**
Decision = Stop → 进入 Q4。
Decision = Continue → 调用已确认的工具，等待结果后重新执行 Q2.6。

**Q4：回答的核心论点是什么？**
用一两句话概括中心结论。

**Q5：关键风险检查**
逐项确认：物理逻辑 / 数学一致性 / 引用充分性 / 代码可执行性。

**Q6：输出规划（必须完整）**
明确列出：
- 回答结构（各部分标题或段落安排）
- 是否包含公式、代码、图示说明
- 引用编号规划（哪些论点对应哪条引用）
- 预期回答长度（简短/中等/详细）
- 语言（跟随用户）
""".format(TOOL_DECISION_PLUGIN)
